<nav class="navbar navbar-default" role="navigation" style="margin: 0; padding: 0; border-radius: 0">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-left" href="{{ path('homepage') }}"><img src="{{ asset('resources/images/logo/FullTransparent.png') }}" style="height: 50px"></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="pull-left"><a href="{{ path("cord_list") }}">Cords</a></li>
                <li class="pull-left"><a href="{{ path("accent_list") }}">Accents</a></li>
                <li class="pull-left"><a href="{{ path("model_list") }}">Models</a></li>
            {% endif %}
                <!-- <a href="{{ path('all_shop') }}"><img src="{{ asset('resources/images/logo/LogoTransparent.png') }}" style="height: 50px"></a> -->
                <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                    <li><a>Welcome, {{ app.user.username }}!</a></li>
                {% endif %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Info<span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('homepage') }}">About Us</a></li>
                            <li><a href="{{ path('all_shop') }}">All Models</a></li>
                            <li><a href="{{ path('cord_catalog') }}">Cord Catalog</a></li>
                            <li><a href="{{ path('faq') }}">FAQ</a></li>
                            <li role="separator" class="divider"></li>
                            {% if app.user %}
                                <li><a href="/parashop/web/logout">Logout</a></li>
                            {% else %}
                                <li><a href="/parashop/web/login">Login</a></li>
                            {% endif %}
                        </ul>
                    </li>
                    <li><a href="{{ path('cart_shop') }}"><i class="glyphicon glyphicon-shopping-cart"></i></a></li>
                </ul>

            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>