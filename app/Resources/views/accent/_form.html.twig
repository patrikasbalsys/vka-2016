{{ form_start(form) }}
    {{ form_row(form.model) }}
    {{ form_row(form.cord_size) }}
    {{ form_row(form.price) }}
    {{ form_row(form.stock) }}
    {{ form_row(form.image) }}



    <button type="submit" class="btn btn-primary">Save</button>
{{ form_end(form) }}